#num = int(input("ENTER >>> "))


def num_method(num):
  num1=0
  temp1=num
  while(num!=0):
      temp = num%10
      num=num//10
      num1=(num1*10)+temp
return "Palindrome" if num1==temp1 else 'not palindrome'

def str_method(x)
return "Palindrome" if str(x)==str(x)[::-1] else 'not palindrome'
